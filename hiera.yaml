---
version: 5
defaults:
  datadir: hieradata
  data_hash: yaml_data
hierarchy:
    - name: "Node YAML"
      paths:
        - "nodes/%{facts.trusted.certname}.yaml"
        - "nodegroups/%{facts.cis_rhel7_nodegroup}.yaml"
        - "common.yaml"
